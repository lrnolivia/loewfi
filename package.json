{
  "name": "loewfi",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "render:fixtures": "tsc -p tsconfig.renderer.json && node .renderer-build/scripts/render-fixtures.js",
    "dev": "pnpm render:fixtures && vite",
    "build": "pnpm render:fixtures && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check": "pnpm typecheck && pnpm test && pnpm build"
  },
  "dependencies": {
    "@sohumsuthar/liquid-glass": "3.1.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.7.0",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@types/node": "^24.3.0",
    "typescript": "^5.9.2",
    "vite": "^6.4.3",
    "vitest": "^3.2.4"
  }
}
